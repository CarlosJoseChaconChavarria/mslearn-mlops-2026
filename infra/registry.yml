# infra/registry.yml
# Template configuration file for provisioning the shared MLOps workspace hub
$schema: https://azuremlschemas.azureedge.net/latest/registry.schema.json

name: REGISTRY_NAME_PLACEHOLDER
location: PRIMARY_REGION_PLACEHOLDER

tags:
  description: Shared registry for approved machine learning assets across workspaces
  tier: shared-assets
  billing: low-cost

replication_locations:
  - location: PRIMARY_REGION_PLACEHOLDER